{
  "name": "pagify-it",
  "description": "Add routing to your React app in one minute.",
  "version": "0.0.4",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sonaye/pagify-it.git"
  },
  "peerDependencies": {
    "page": "^1.7.1"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-preset-env": "1.6.1",
    "babel-preset-react-app": "3.1.0",
    "babel-preset-stage-0": "6.24.1"
  },
  "scripts": {
    "build":
      "rm -r -f lib && mkdir -p lib && BABEL_ENV=production babel src --out-dir lib"
  },
  "babel": {
    "presets": [
      ["env", { "targets": { "node": "current" } }],
      "react-app",
      "stage-0"
    ]
  }
}
