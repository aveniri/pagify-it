{
  "name": "pagify-it",
  "description": "Add routing to your React app in one minute.",
  "version": "0.0.2",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sonaye/pagify-it.git"
  },
  "peerDependencies": {
    "page": "^1.7.1"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-preset-react-app": "3.1.0",
    "cross-env": "5.1.3"
  },
  "scripts": {
    "build":
      "mkdir -p lib && cross-env NODE_ENV=production babel src --out-file lib/index.js --presets=react-app"
  }
}
