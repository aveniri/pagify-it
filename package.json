{
  "license": "MIT",
  "name": "pagify-it",
  "description": "Add routing to your React app in one minute.",
  "version": "0.0.6",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sonaye/pagify-it.git"
  },
  "peerDependencies": {
    "page": "^1.8.6"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-preset-es2015": "6.24.1",
    "babel-preset-react-app": "3.1.1",
    "babel-preset-stage-0": "6.24.1"
  },
  "scripts": {
    "build":
      "mkdir -p lib && NODE_ENV=production babel src --out-dir lib --presets=es2015,stage-0,react-app --watch"
  }
}
